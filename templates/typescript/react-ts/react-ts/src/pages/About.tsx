import * as React from 'react';

export class About extends React.Component {
    public render() {
        return (
            <div>Quicksi can code, can dance, loves coffee too, quicksi has can loves developers.</div>
        );
    }
}